{
  "name": "gemini-key-pool",
  "version": "1.0.0",
  "description": "Gemini API密钥管理与自动分配系统",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "mkdir -p dist && cp -r public/* dist/ && cp server.js dist/ && cp package*.json dist/ && cp -r routes dist/ && cp -r controllers dist/ && cp -r services dist/ && cp -r middleware dist/"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "jsonwebtoken": "^9.0.2",
    "axios": "^1.6.0",
    "cloudflare": "^3.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
