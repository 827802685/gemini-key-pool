{
  "name": "gemini-key-pool",
  "version": "1.0.0",
  "description": "Gemini API密钥号池系统",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "mkdir -p dist && cp -r public/* dist/ && cp server.js dist/ && cp -r routes dist/ && cp -r services dist/ && cp -r middleware dist/ && cp package.json dist/ && cp package-lock.json dist/"
  },
  "dependencies": {
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "axios": "^1.6.2",
    "cloudflare": "^3.2.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
    